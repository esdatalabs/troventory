// Package export is the top-level package for the export feature,
// exposing the Dispatcher that acceptance tests and callers use as the
// feature's single entry point (ARCHITECTURE.md §4).
package export
