# Troventory dashboard

A Vue 3 + Vite single-page app for demoing Troventory in a browser: locations, items,
valuation, search, and export, all driven through `cmd/api`'s REST API.

See the root [`README.md`](../README.md#using-the-web-dashboard) for how to run it alongside
the API.
